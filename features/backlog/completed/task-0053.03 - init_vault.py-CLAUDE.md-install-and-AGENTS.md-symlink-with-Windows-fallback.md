---
id: TASK-0053.03
title: 'init_vault.py: CLAUDE.md install and AGENTS.md symlink with Windows fallback'
status: Done
assignee: []
created_date: '2026-05-28 16:07'
updated_date: '2026-05-28 17:03'
labels:
  - portability
milestone: Vault portability
dependencies: []
modified_files:
  - init_vault.py
parent_task_id: TASK-0053
priority: high
ordinal: 300
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Add CLAUDE.md installation and AGENTS.md creation to `init_vault.py`.

CLAUDE.md: prompt user before overwriting if it already exists (same logic as init-vault.sh: default is keep, `y` to overwrite). Use `input()` for the prompt.

AGENTS.md: attempt `os.symlink("CLAUDE.md", str(agents_path))` first. On any `(OSError, NotImplementedError, PermissionError)` — which covers Windows without Developer Mode — silently fall back to `shutil.copy2(claude_path, agents_path)`. No warning message; the copy is a valid equivalent.

Skip AGENTS.md creation if the file already exists (same as init-vault.sh `if [ ! -e ]` check).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 CLAUDE.md is copied from script_dir to vault_dir
- [ ] #2 If CLAUDE.md already exists, user is prompted; skipped unless user confirms
- [ ] #3 AGENTS.md is created as a symlink on platforms that support it (Linux, macOS, WSL)
- [ ] #4 AGENTS.md falls back to a copy on Windows without Developer Mode — no exception raised, no warning printed
- [ ] #5 AGENTS.md is skipped if it already exists
<!-- AC:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added install_claude_md() with prompt-before-overwrite logic and AGENTS.md symlink/copy fallback. Symlinks work on this Windows system (Developer Mode enabled). Idempotency verified. Committed: 9a7c011.
<!-- SECTION:FINAL_SUMMARY:END -->
