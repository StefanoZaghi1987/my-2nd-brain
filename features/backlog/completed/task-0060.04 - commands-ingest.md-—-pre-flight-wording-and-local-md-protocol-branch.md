---
id: TASK-0060.04
title: commands/ingest.md — pre-flight wording and local-md protocol branch
status: Done
assignee: []
created_date: '2026-05-29 06:57'
updated_date: '2026-05-29 07:56'
labels: []
dependencies: []
documentation:
  - features/specs/2026-05-29-markdown-drop-ingest-design.md
modified_files:
  - commands/ingest.md
parent_task_id: TASK-0060
ordinal: 4000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Update `commands/ingest.md` with two changes:

**1. Pre-flight block (step 2) — wording update only:**
Change "Scan for `*.pdf` files in the drop zone" to:
> Scan for files with supported types (`.pdf`, `.md`) in the drop zone.

No other changes to the pre-flight block.

**2. New source type branch — `local-md`:**
Add alongside the existing `local-pdf` branch under the Protocol section:

```
### Local Markdown files

Source: `raw/local/<slug>/index.md` with `fetch_method: local-md`.

1. Read `index.md` — get `title`, `source_url` (if present), `tags`, `note`.
2. Read `content.md` in full (plain text; no page limit).
3. Infer real title from content if better than index.md title
   (first H1 heading takes precedence over the filename-derived title).
4. Write `wiki/sources/<slug>.md`:
   - Include `source_url` only if present in `index.md`.
   - `source_path: raw/local/<slug>/`
   - `fetch_method: local-md`
5. Propagate `tags` and `note` as with other source types.
```

**Context:** `commands/ingest.md` is an LLM instruction file (no code). Read it carefully before editing — the existing `local-pdf` branch (lines ~78-94) is the direct model to follow.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Pre-flight step 2 mentions .md alongside .pdf
- [ ] #2 local-md protocol branch present under the PDFs section
- [ ] #3 local-md branch instructs reading content.md in full (no page limit)
- [ ] #4 local-md branch instructs omitting source_url when not present in index.md
- [ ] #5 local-md branch instructs using source_path: raw/local/<slug>/ and fetch_method: local-md in wiki source frontmatter
- [ ] #6 Existing local-pdf branch is unchanged
<!-- AC:END -->
