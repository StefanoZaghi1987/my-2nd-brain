---
id: TASK-0053.02
title: 'init_vault.py: folder structure — directory creation and gitkeep files'
status: Done
assignee: []
created_date: '2026-05-28 16:07'
updated_date: '2026-05-28 17:01'
labels:
  - portability
milestone: Vault portability
dependencies: []
modified_files:
  - init_vault.py
parent_task_id: TASK-0053
priority: high
ordinal: 200
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Add the folder structure creation step to `init_vault.py`. Creates all required directories and `.gitkeep` files, matching the behaviour of init-vault.sh exactly.

Directories to create (all relative to vault_dir):
`raw/papers`, `raw/web`, `wiki/pages`, `wiki/sources`, `wiki/views/assets`, `conversations`, `.lint`, `.claude/skills/inbox-fetcher/scripts`, `.claude/skills/vault-linter/scripts`, `.claude/skills/view-builder/templates`, `.claude/skills/shared`, `.claude/commands`, `.obsidian`

Gitkeep files (touch if absent) in: `raw/papers`, `raw/web`, `wiki/pages`, `wiki/sources`, `wiki/views`, `wiki/views/assets`, `conversations`, `.lint`

All operations must be idempotent (`exist_ok=True`, skip gitkeep if present).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 All directories exist after running on a clean path
- [ ] #2 All gitkeep files exist in the correct locations
- [ ] #3 Re-running on an existing vault produces no errors and prints skip() for existing items
- [ ] #4 Uses pathlib.Path.mkdir(parents=True, exist_ok=True)
<!-- AC:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added DIRS/GITKEEP_DIRS constants and create_dirs(). Also added UTF-8 stdout reconfiguration at startup to handle Unicode symbols (✓, ⚠, ✗) on Windows without PYTHONIOENCODING env var. All directories and .gitkeep files verified. Committed: b241843.
<!-- SECTION:FINAL_SUMMARY:END -->
