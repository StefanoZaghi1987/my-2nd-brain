---
id: TASK-0080.04
title: Normalize Python launcher callout in all command docs (B1)
status: Done
assignee: []
created_date: '2026-05-29 18:48'
updated_date: '2026-05-29 20:23'
labels:
  - portability
  - docs
milestone: Phase 4 – Hardening &amp; Portability
dependencies:
  - TASK-0080.03
references:
  - features/plans/2026-05-29-llm-wiki-hardening.md
modified_files:
  - commands/fetch.md
  - commands/lint.md
  - commands/ingest.md
  - commands/refresh.md
  - commands/merge.md
parent_task_id: TASK-0080
priority: medium
ordinal: 400
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
**Why:** Command docs hardcode `python3` in bash blocks (`fetch.md`, `lint.md`, `ingest.md`, `refresh.md`) or use bare `python` (`merge.md`). On stock Windows, `python3` does not resolve — the correct command is `python`. `init_vault.py:439` already knows this (`py_cmd = "python3" if os.name != "nt" else "python"`) but the docs were never updated. The LLM executor adapts at runtime, but inconsistent docs erode clarity and waste LLM reasoning.

**Repo context:** `D:\my-2nd-brain` is the engine/template repo. Command `.md` files are copied verbatim to `.claude/commands/` in deployed vaults.

**Change pattern** — add this callout immediately before the first ` ```bash ` block in each file:
```
> **Windows:** replace `python3` with `python` in all commands below.
```

Also: in `commands/merge.md`, change bare `python .claude/skills/...` → `python3 .claude/skills/...` in the find_backlinks.py invocation blocks (MERGE Protocol step 4).

**Files to modify:** `commands/fetch.md`, `commands/lint.md`, `commands/ingest.md`, `commands/refresh.md`, `commands/merge.md`

Note: `commands/split.md` (created in TASK-0080.03), `commands/review.md` (updated in TASK-0080.02), and `commands/retry.md` (created in TASK-0080.07) already have or will have this callout.

**Exact step-by-step in Plan Task 4** of `features/plans/2026-05-29-llm-wiki-hardening.md`.

**Dependency:** Run after TASK-0080.03 (that task modifies merge.md — sequential to avoid conflicts).

**Documentation-only change.** No Python code modified. No new tests needed.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 commands/fetch.md has the Windows callout before the first bash block
- [x] #2 commands/lint.md has the Windows callout before the first bash block
- [x] #3 commands/ingest.md has the Windows callout before the first bash block
- [x] #4 commands/refresh.md has the Windows callout before the first bash block
- [x] #5 commands/merge.md has the Windows callout and uses python3 (not bare python) in the find_backlinks.py invocation blocks
- [x] #6 No Python scripts were modified
<!-- AC:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added `> **Windows:** replace \`python3\` with \`python\` in all commands below.` callout before the first code block in `fetch.md`, `lint.md`, `ingest.md`, and `refresh.md`. In `merge.md`: added the same callout and normalized bare `python .claude/skills/...` occurrences in code blocks to `python3`. Documentation-only; no Python code modified. `commands/split.md`, `commands/review.md`, and `commands/retry.md` were excluded — they already carried the Windows note from Tasks 2, 3, and 7 respectively. 153/153 tests pass. Committed: a9ddcad.
<!-- SECTION:FINAL_SUMMARY:END -->
