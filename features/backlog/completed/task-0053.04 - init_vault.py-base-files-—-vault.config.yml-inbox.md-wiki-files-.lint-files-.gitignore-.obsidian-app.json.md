---
id: TASK-0053.04
title: >-
  init_vault.py: base files — vault.config.yml, inbox.md, wiki files, .lint
  files, .gitignore, .obsidian/app.json
status: Done
assignee: []
created_date: '2026-05-28 16:07'
updated_date: '2026-05-28 17:04'
labels:
  - portability
milestone: Vault portability
dependencies: []
modified_files:
  - init_vault.py
parent_task_id: TASK-0053
priority: high
ordinal: 400
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Add creation of all base files to `init_vault.py`. Every file is only created if absent (idempotent). Content must be byte-for-byte identical to the heredocs in init-vault.sh.

Files to create:
- `vault.config.yml` — copy from script_dir/vault.config.yml
- `inbox.md` — hardcoded content (URL queue template)
- `wiki/index.md` — hardcoded content (catalog template)
- `wiki/log.md` — hardcoded content (append-only log template)
- `wiki/hot.md` — content: `## [INIT]\n\nVault just bootstrapped. No sessions yet.\n` (no frontmatter — matches /hot output format)
- `.lint/state.yaml` — hardcoded content (last_lint: null, etc.)
- `.lint/report.md` — hardcoded content (no lint run yet message)
- `.gitignore` — hardcoded content (matching init-vault.sh heredoc)
- `.obsidian/app.json` — hardcoded content (useMarkdownLinks, newLinkFormat, etc.)

Note: wiki/hot.md content intentionally has NO YAML frontmatter (unlike current init-vault.sh which still has the old template — this task and task-0052 both correct that).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 All 9 base files exist after running on a clean path
- [ ] #2 Re-running skips all files that already exist with a skip() message
- [ ] #3 wiki/hot.md contains no YAML frontmatter
- [ ] #4 wiki/hot.md initial content matches: `## [INIT]\n\nVault just bootstrapped. No sessions yet.`
- [ ] #5 vault.config.yml is copied from script_dir (not hardcoded), preserving user edits on re-run
- [ ] #6 All files written with encoding='utf-8'
<!-- AC:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added all base file content constants and write_base_files(). Critical checks: _LINT_STATE uses fetches_since_last_lint: 0 ✓, _HOT_MD has no frontmatter ✓, _LINT_REPORT references python3 .claude/skills/vault-linter/scripts/lint.py ✓. Committed: 1912444.
<!-- SECTION:FINAL_SUMMARY:END -->
