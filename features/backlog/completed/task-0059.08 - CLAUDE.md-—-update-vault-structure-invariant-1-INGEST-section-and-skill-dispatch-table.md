---
id: TASK-0059.08
title: >-
  CLAUDE.md — update vault structure, invariant #1, INGEST section, and skill
  dispatch table
status: Done
assignee: []
created_date: '2026-05-28 21:09'
updated_date: '2026-05-28 22:15'
labels:
  - docs
  - CLAUDE.md
dependencies:
  - TASK-0059.07
references:
  - features/plans/2026-05-28-copy-paste-pdf-ingestion-plan.md#task-8
modified_files:
  - CLAUDE.md
parent_task_id: TASK-0059
ordinal: 8000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Update `CLAUDE.md` to reflect the new drop zone architecture in four places.

**1. Vault structure diagram** — add two lines under `raw/`:
```
raw/local/<slug>/       PDFs copy-pasted by the user
raw/drop/               Drop zone — paste PDFs here; emptied by /ingest
```
Also update `raw/papers/` label to `PDFs fetched via URL`.

**2. Invariant #1** — current text: `Only the fetcher skill adds files there.`
New text: `Only scripts add files there: fetch_inbox.py writes to raw/papers/ and raw/web/; adopt_drop.py writes to raw/local/. The LLM never writes to raw/.`

**3. INGEST — source type branches** — add a new **Local PDFs** branch after the existing PDFs branch covering `raw/local/<slug>/index.md` with `fetch_method: local-pdf`. Key difference: no `source_url`; `source_path: raw/local/<slug>/`.

**4. Skill dispatch table** — add row: `| ADOPT | adopt_drop.py | scripts/adopt_drop.py |`

**Dependency:** TASK-0059.07 (ingest command updated first to keep CLAUDE.md consistent with commands/).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 CLAUDE.md vault structure diagram shows raw/local/ and raw/drop/ with accurate descriptions
- [x] #2 Invariant #1 names both fetch_inbox.py and adopt_drop.py as the permitted raw/ writers
- [x] #3 INGEST section has a Local PDFs source type branch that omits source_url and uses source_path: raw/local/<slug>/
- [x] #4 Skill dispatch table has an ADOPT row pointing to adopt_drop.py
<!-- AC:END -->
