---
id: TASK-0080.07
title: Add /retry command for failed inbox URLs (E1)
status: Done
assignee: []
created_date: '2026-05-29 18:49'
updated_date: '2026-05-29 20:23'
labels:
  - feature
  - tdd
milestone: Phase 4 – Hardening &amp; Portability
dependencies:
  - TASK-0080.05
references:
  - features/plans/2026-05-29-llm-wiki-hardening.md
  - skills/inbox-fetcher/scripts/fetch_inbox.py
modified_files:
  - skills/inbox-fetcher/scripts/fetch_inbox.py
  - commands/retry.md
  - init_vault.py
  - tests/test_fetch_inbox.py
  - tests/test_installer.py
parent_task_id: TASK-0080
priority: high
ordinal: 700
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
**Why:** When `fetch_inbox.py` fails to fetch a URL, it appends `⚠ reason` to that line in inbox.md (e.g. `- [ ] https://example.com ⚠ connection timeout`). The existing `UNCHECKED_PATTERN` regex (`^- \[ \] (https?://\S+)\s*$`) no longer matches these lines because they no longer end at the URL. The only workaround is manually deleting the `⚠ ...` text before retrying — error-prone and annoying. This task adds a `FAILED_PATTERN`, a `find_failed_entries()` function, extends `update_inbox()` to handle ⚠-marked lines, and adds a `--retry` CLI mode. A new `commands/retry.md` completes the feature.

**Repo context:** `D:\my-2nd-brain` is the engine/template repo. `fetch_inbox.py` is at `skills/inbox-fetcher/scripts/fetch_inbox.py`. After vault install, it lives at `.claude/skills/inbox-fetcher/scripts/fetch_inbox.py`.

**Files to modify:**
- `skills/inbox-fetcher/scripts/fetch_inbox.py`:
  - Add `FAILED_PATTERN = re.compile(r"^- \[ \] (https?://\S+) ⚠ .+$")` after line 81
  - Add `find_failed_entries(inbox_text: str) -> list[InboxEntry]` (same structure as `find_unchecked_entries` but uses FAILED_PATTERN; preserves sub-bullet tags/note)
  - In `update_inbox()`: after the `UNCHECKED_PATTERN.match(line)` check, add a secondary `FAILED_PATTERN.match(line)` fallback so retried ⚠-lines get updated (cleared on success, fresh reason on failure)
  - `process_vault(vault, dry_run, retry=False)`: when `retry=True`, call `find_failed_entries` instead of `find_unchecked_entries`
  - `main()`: add `--retry` argument; pass `retry=args.retry` to `process_vault`
- `init_vault.py`: add `"retry"` to `COMMANDS` list (after `"split"`)
- `tests/test_fetch_inbox.py`: add `TestFailedPattern`, `TestFindFailedEntries`, `TestUpdateInboxRetry` classes
- `tests/test_installer.py`: extend `TestCommands` with `test_retry_in_commands` and `test_retry_md_exists`

**Files to create:**
- `commands/retry.md` — protocol file: when to use, how to run (`--retry` flag), what it does/doesn't do

**All exact code, test cases, and retry.md content is in Plan Task 7** of `features/plans/2026-05-29-llm-wiki-hardening.md`.

**Dependency:** Run after TASK-0080.05 (both touch `init_vault.py` — sequential to avoid conflicts).

**TDD:** Write 3 new test classes in `test_fetch_inbox.py` → run (fail) → implement FAILED_PATTERN + find_failed_entries + update_inbox extension + --retry mode → run (pass) → create retry.md + update COMMANDS → extend test_installer.py → run all tests → commit.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 FAILED_PATTERN = re.compile(r'^- \[ \] (https?://\S+) ⚠ .+$') exists in fetch_inbox.py
- [x] #2 find_failed_entries(inbox_text) returns InboxEntry list for ⚠-marked lines only (ignores plain unchecked and [x] lines)
- [x] #3 find_failed_entries preserves sub-bullet tags and note fields
- [x] #4 update_inbox() clears the ⚠ marker and moves to Processed when a retry succeeds
- [x] #5 update_inbox() replaces the ⚠ reason with the new reason when a retry fails
- [x] #6 fetch_inbox.py --retry mode re-attempts only ⚠-marked entries, leaving plain unchecked and [x] entries untouched
- [x] #7 commands/retry.md exists with description: frontmatter, how-to-run, and what it does NOT do sections
- [x] #8 init_vault.py COMMANDS includes 'retry'
- [x] #9 tests/test_fetch_inbox.py has TestFailedPattern, TestFindFailedEntries, TestUpdateInboxRetry all passing
- [x] #10 tests/test_installer.py TestCommands has test_retry_in_commands and test_retry_md_exists both passing
- [x] #11 python -m pytest -v full suite passes
<!-- AC:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added `FAILED_PATTERN = re.compile(r"^- \[ \] (https?://\S+) ⚠ .+$")` to `fetch_inbox.py` after `UNCHECKED_PATTERN`. The two patterns cleanly partition the inbox: `UNCHECKED_PATTERN`'s `\s*$` anchor never matches a `⚠`-suffixed line; `FAILED_PATTERN` only matches those lines. Added `find_failed_entries()` (mirrors `find_unchecked_entries` but uses `FAILED_PATTERN`; preserves tags/note sub-bullets for retry context). Extended `update_inbox()` to check `FAILED_PATTERN` when `UNCHECKED_PATTERN` fails — enabling ⚠-marked URLs to be cleared on retry success or have their reason updated on retry failure. Added `retry: bool = False` param to `process_vault()` and `--retry` flag to `main()`. Created `commands/retry.md`. Added `"retry"` to `init_vault.py:COMMANDS`. Extended `tests/test_fetch_inbox.py` with `TestFailedPattern` (3), `TestFindFailedEntries` (5), `TestUpdateInboxRetry` (2). Extended `tests/test_installer.py` with 2 retry tests. 165/165 tests pass. Committed: 60cceb5.
<!-- SECTION:FINAL_SUMMARY:END -->
