---
id: TASK-0080.06
title: Fix chart.py output path to wiki/views/assets/ (B3)
status: To Do
assignee: []
created_date: '2026-05-29 18:49'
labels:
  - portability
  - tdd
milestone: Phase 4 – Hardening &amp; Portability
dependencies: []
references:
  - features/plans/2026-05-29-llm-wiki-hardening.md
  - skills/view-builder/templates/chart.py
modified_files:
  - skills/view-builder/templates/chart.py
  - tests/test_chart.py
parent_task_id: TASK-0080
priority: medium
ordinal: 600
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
**Why:** `skills/view-builder/templates/chart.py` writes `OUTPUT_DIR = Path(__file__).parent / "assets"`, which places generated PNGs next to the template file at `.claude/skills/view-builder/templates/assets/` in a deployed vault. The correct location is `wiki/views/assets/` (where all view assets live). The fix accepts `--vault` as an argument (matching the convention of `fetch_inbox.py`, `lint.py`, etc.) and resolves the output directory from the vault root.

**Repo context:** `D:\my-2nd-brain` is the engine/template repo. `chart.py` is a template the LLM adapts per chart request; it is copied to `.claude/skills/view-builder/templates/chart.py` by `init_vault.py`. The agent runs it from the vault root.

**File to modify:** `skills/view-builder/templates/chart.py`
- Wrap chart generation in `build_chart(vault_root, output_name)` function
- `main()` adds `argparse` with `--vault` (default `Path.cwd()`) and `--output` args
- `output_dir = vault_root / "wiki" / "views" / "assets"`

**File to create:** `tests/test_chart.py`
- `test_writes_to_vault_views_assets` — calls `chart.py --vault <tmp>`, asserts PNG at `<tmp>/wiki/views/assets/chart.png`
- `test_does_not_write_next_to_template` — asserts no PNG at `skills/view-builder/templates/assets/chart.png`

**Requires `matplotlib`:** `pip install matplotlib` (part of the phase setup).

**All exact code in Plan Task 6** of `features/plans/2026-05-29-llm-wiki-hardening.md`.

**Independent task** — no dependency on other tasks in this phase.

**TDD:** Write `tests/test_chart.py` → run (fails: PNG in wrong location) → update chart.py → run (passes) → commit.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 chart.py accepts --vault <path> argument (default: cwd)
- [ ] #2 chart.py writes PNG to <vault>/wiki/views/assets/chart.png
- [ ] #3 chart.py does NOT write to skills/view-builder/templates/assets/
- [ ] #4 tests/test_chart.py exists with both tests passing
- [ ] #5 python -m pytest tests/test_chart.py -v passes
<!-- AC:END -->
