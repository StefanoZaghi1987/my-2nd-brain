---
id: TASK-0059.07
title: >-
  commands/ingest.md — add drop zone pre-flight block and local-pdf protocol
  branch
status: To Do
assignee: []
created_date: '2026-05-28 21:09'
labels:
  - commands
  - ingest
dependencies:
  - TASK-0059.04
references:
  - features/plans/2026-05-28-copy-paste-pdf-ingestion-plan.md#task-7
modified_files:
  - commands/ingest.md
parent_task_id: TASK-0059
ordinal: 7000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Update `commands/ingest.md` so that `/ingest` (1) runs `adopt_drop.py` as a pre-flight step when the drop zone contains PDFs, and (2) handles `fetch_method: local-pdf` sources (no `source_url`, `source_path: raw/local/<slug>/`).

**Two changes required:**

1. **Pre-flight section** — insert before `## Discover targets`:
   - Check `drop_zone.path` from config
   - If PDFs present, run `python3 skills/inbox-fetcher/scripts/adopt_drop.py --vault <vault_root>`
   - Report adopted slugs
   - Ask once (batch): "Any tags or focus notes before I ingest these?"
   - If user provides, update `raw/local/<slug>/index.md` frontmatter before reading
   - Skip silently if `drop_zone.enabled: false`

2. **local-pdf branch** in `### PDFs` section — extend source detection to include `raw/local/<slug>/index.md` with `fetch_method: local-pdf`, and specify that `source_url` is omitted from `wiki/sources/<slug>.md` for this type.

**Can be done in parallel with TASK-0059.05 and TASK-0059.06** — no code dependencies.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 commands/ingest.md has a Pre-flight section before Discover targets describing the adopt_drop.py invocation
- [ ] #2 The pre-flight section covers the interactive tags/notes prompt and the skip-if-disabled condition
- [ ] #3 The PDFs section covers both raw/papers/ (fetch_method: pdf) and raw/local/ (fetch_method: local-pdf) as valid source locations
- [ ] #4 The local-pdf branch explicitly states that source_url is omitted from wiki/sources/<slug>.md
- [ ] #5 The local-pdf branch specifies source_path: raw/local/<slug>/ in the wiki source frontmatter
<!-- AC:END -->
