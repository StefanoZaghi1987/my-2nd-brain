---
id: TASK-0080.03
title: Create standalone /split command file and fix installer (A1)
status: To Do
assignee: []
created_date: '2026-05-29 18:48'
labels:
  - correctness
  - tdd
milestone: Phase 4 – Hardening &amp; Portability
dependencies:
  - TASK-0080.01
references:
  - features/plans/2026-05-29-llm-wiki-hardening.md
  - commands/merge.md
  - init_vault.py
modified_files:
  - commands/split.md
  - commands/merge.md
  - init_vault.py
  - tests/test_installer.py
parent_task_id: TASK-0080
priority: high
ordinal: 300
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
**Why:** `CLAUDE.md` advertises `/split <page> <new-A> <new-B>` as a slash command, but `commands/split.md` does not exist and `init_vault.py:COMMANDS` (lines 87-90) omits `"split"` — so the command is never deployed to vaults. The SPLIT protocol currently lives buried inside `commands/merge.md`. This task extracts it into a standalone, self-contained file and wires the installer so it deploys.

**Repo context:** `D:\my-2nd-brain` is the engine/template repo. `init_vault.py:install_commands()` copies every file listed in `COMMANDS` from `commands/<name>.md` to `.claude/commands/<name>.md` in the target vault. If `"split"` is absent from `COMMANDS`, the file is never copied.

**Files to create:**
- `commands/split.md` — full SPLIT protocol (7 steps + Guards + Unattended mode + Report format, self-contained so the LLM needs only this one file)
- `tests/test_installer.py` (NEW) — `TestCommands` class with `test_split_in_commands` and `test_split_md_exists`

**Files to modify:**
- `commands/merge.md` — remove the `## SPLIT Protocol` section; add cross-link to `/split`; update description frontmatter to MERGE-only
- `init_vault.py` — add `"split"` to the `COMMANDS` list (lines 87-90)

**All exact content for `split.md`, the merge.md edits, init_vault.py change, and test class is in Plan Task 2** of `features/plans/2026-05-29-llm-wiki-hardening.md`.

**Dependency:** Run after TASK-0080.01 (both touch `init_vault.py` — sequential to avoid conflicts).

**TDD:** Write `tests/test_installer.py::TestCommands` with split tests → run to confirm failure → create split.md + edit merge.md + add "split" to COMMANDS → run to confirm pass → commit.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 commands/split.md exists with the full self-contained SPLIT protocol (7 steps, Guards, Unattended mode, Report format)
- [ ] #2 commands/split.md has a description: frontmatter declaring it as the split command
- [ ] #3 commands/merge.md no longer contains the ## SPLIT Protocol section
- [ ] #4 commands/merge.md contains a cross-link reference pointing to /split
- [ ] #5 init_vault.py COMMANDS list includes 'split'
- [ ] #6 tests/test_installer.py exists with test_split_in_commands and test_split_md_exists both passing
- [ ] #7 python -m pytest tests/test_installer.py -v passes (split tests only; retry tests added later in TASK-0080.07)
<!-- AC:END -->
