---
id: TASK-0059.02
title: adopt_drop.py — slug and title helper functions (TDD)
status: To Do
assignee: []
created_date: '2026-05-28 21:08'
labels:
  - adopt-drop
  - tdd
dependencies:
  - TASK-0059.01
references:
  - features/plans/2026-05-28-copy-paste-pdf-ingestion-plan.md#task-2
modified_files:
  - skills/inbox-fetcher/scripts/adopt_drop.py
  - tests/test_adopt_drop.py
parent_task_id: TASK-0059
ordinal: 2000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Create the new script `skills/inbox-fetcher/scripts/adopt_drop.py` with just the two pure helper functions: `slug_from_filename` and `title_from_slug`. No filesystem side-effects. Follow TDD: write tests first.

**Context:** The script mirrors `fetch_inbox.py` in structure. It needs `python-slugify` (already a project dependency). The sys.path insert pattern for shared imports is `sys.path.insert(0, str(Path(__file__).parent.parent.parent / "shared"))`.

**Dependency:** TASK-0059.01 must be done first (vault_state import is in the file header).

**`slug_from_filename("attention-is-all-you-need.pdf")` → `"attention-is-all-you-need"`**
**`title_from_slug("attention-is-all-you-need")` → `"Attention Is All You Need"`**
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 slug_from_filename strips the .pdf extension and slugifies the stem
- [ ] #2 slug_from_filename handles spaces, underscores, mixed case, and parentheses without returning dots in the result
- [ ] #3 title_from_slug converts hyphens and underscores to spaces and title-cases each word
- [ ] #4 title_from_slug returns empty string for empty input
- [ ] #5 Test file tests/test_adopt_drop.py is created with TestSlugFromFilename and TestTitleFromSlug classes
- [ ] #6 pytest tests/test_adopt_drop.py::TestSlugFromFilename tests/test_adopt_drop.py::TestTitleFromSlug passes
<!-- AC:END -->
