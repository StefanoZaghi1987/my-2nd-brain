---
id: TASK-0080.08
title: Sync CLAUDE.md and README.md with code reality (C1-C4)
status: To Do
assignee: []
created_date: '2026-05-29 18:49'
labels:
  - docs
milestone: Phase 4 – Hardening &amp; Portability
dependencies:
  - TASK-0080.03
  - TASK-0080.07
references:
  - features/plans/2026-05-29-llm-wiki-hardening.md
  - CLAUDE.md
  - README.md
modified_files:
  - CLAUDE.md
  - README.md
parent_task_id: TASK-0080
priority: medium
ordinal: 800
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
**Why:** `CLAUDE.md` has four discrepancies with the actual implementation, and `README.md`'s commands tree is stale after this phase's changes. All are pure documentation fixes — no code changes.

**Problems to fix:**

**C1 — Linter check count:** CLAUDE.md line ~224 says "Deterministic checks only (dead links, missing frontmatter, naming consistency, view staleness)" — only 4 checks mentioned. `lint.py` implements **14** (`all_checks` list, lines 787-802). Fix the count and list.

**C2 — Missing slash commands:** CLAUDE.md's `## Slash commands` section (lines ~314-325) omits `/ingest`, `/lint`, `/split`, `/retry`. Add all four in logical order.

**C3 — Command path prefixes:** Four bare `commands/*.md` references in the Operations section (lines ~210, 230, 238, 247) don't resolve after install (they live at `.claude/commands/`). Prefix each with `.claude/`.

**C4 — MERGE/SPLIT reference:** A CLAUDE.md line that says the SPLIT protocol lives inside `merge.md` must now point to the new `split.md` (created in TASK-0080.03).

**README.md — Commands tree update:**
- Line ~42: `├── merge.md    /merge, /split` → split into two lines (`merge.md /merge` and `split.md /split`)
- Add `├── retry.md    /retry` to the tree

**Files to modify:** `CLAUDE.md`, `README.md`

**Exact find-and-replace text for every change is in Plan Task 8** (steps 1-6) of `features/plans/2026-05-29-llm-wiki-hardening.md`.

**Dependencies:** Run after TASK-0080.03 (split.md must exist before referencing it) and TASK-0080.07 (retry.md must exist before adding /retry to slash-command list).

**Documentation-only.** No Python code changed. No new tests needed. Verify by reading both files after edits.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 CLAUDE.md lint description mentions 14 checks and lists all 14 by name (or points to vault-linter/SKILL.md)
- [ ] #2 CLAUDE.md Slash commands section includes /ingest, /lint, /split, and /retry
- [ ] #3 CLAUDE.md Operations section command references use .claude/commands/ prefix (not bare commands/)
- [ ] #4 CLAUDE.md MERGE/SPLIT reference points to .claude/commands/split.md for the SPLIT protocol
- [ ] #5 README.md commands tree has separate merge.md (/merge) and split.md (/split) entries
- [ ] #6 README.md commands tree includes retry.md (/retry)
- [ ] #7 No Python scripts were modified
- [ ] #8 python -m pytest -v still passes after these doc-only changes
<!-- AC:END -->
