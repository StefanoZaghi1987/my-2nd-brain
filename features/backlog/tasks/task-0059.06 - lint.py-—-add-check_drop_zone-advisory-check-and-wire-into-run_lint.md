---
id: TASK-0059.06
title: lint.py — add check_drop_zone advisory check and wire into run_lint
status: To Do
assignee: []
created_date: '2026-05-28 21:09'
labels:
  - lint
  - tdd
dependencies:
  - TASK-0059.05
references:
  - features/plans/2026-05-28-copy-paste-pdf-ingestion-plan.md#task-6
modified_files:
  - skills/vault-linter/scripts/lint.py
  - tests/test_lint.py
parent_task_id: TASK-0059
ordinal: 6000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Add a new `check_drop_zone(vault: Path) -> list[Finding]` function to `skills/vault-linter/scripts/lint.py` that produces an advisory finding when `.pdf` files are present in the drop zone.

**Context:**
- The function reads `drop_zone.path` from `load_config(vault)` (added in TASK-0059.01)
- Only `.pdf` files count — non-PDF files in the drop zone are ignored
- One finding covers all PDFs; the detail string includes the count: e.g. `"Drop zone has 2 unprocessed file(s) — run /ingest to adopt them."`
- Must be wired into `run_lint`'s `all_checks` list as `("drop_zone", check_drop_zone)`
- The existing dispatch branch that handles `vault`-only functions is `elif name in ("pdf_index", "conversations"):` — add `"drop_zone"` to this tuple

**Dependency:** TASK-0059.01 (config defaults), TASK-0059.05 (lint.py already modified — apply after to avoid conflicts).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 check_drop_zone returns [] when raw/drop/ does not exist
- [ ] #2 check_drop_zone returns [] when raw/drop/ is empty or contains only non-PDF files
- [ ] #3 check_drop_zone returns a single advisory Finding with check='drop_zone_not_empty' when at least one .pdf is present
- [ ] #4 The finding detail string contains the count of unprocessed PDF files
- [ ] #5 check_drop_zone is wired into run_lint and appears in the .lint/report.md output
- [ ] #6 pytest tests/test_lint.py::TestCheckDropZone passes
- [ ] #7 Full pytest tests/ passes
<!-- AC:END -->
