---
id: TASK-0059.11
title: 'init_vault.py — add raw/local/, raw/drop/ dirs and install adopt_drop.py'
status: To Do
assignee: []
created_date: '2026-05-28 21:10'
labels:
  - bootstrap
  - init
dependencies:
  - TASK-0059.04
  - TASK-0059.08
references:
  - features/plans/2026-05-28-copy-paste-pdf-ingestion-plan.md#task-11
modified_files:
  - init_vault.py
parent_task_id: TASK-0059
ordinal: 11000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Update `init_vault.py` so newly bootstrapped vaults include the drop zone and local PDF directories, and so `adopt_drop.py` is installed alongside `fetch_inbox.py`.

**Three changes:**

1. **`DIRS` list** (line ~63) — add `"raw/local"` and `"raw/drop"`
2. **`GITKEEP_DIRS` list** (line ~79) — add `"raw/local"` and `"raw/drop"` so `.gitkeep` files are created
3. **`install_skills` function** (line ~314) — extend the `inbox-fetcher` entry to include `adopt_drop.py`:
   ```python
   ("inbox-fetcher", ["scripts/fetch_inbox.py", "scripts/adopt_drop.py"]),
   ```
4. **`print_done` banner** — update step 2 from `"drop PDFs in raw/papers/"` to `"drop PDFs in raw/drop/"`

**Dependency:** TASK-0059.04 (adopt_drop.py must exist before it can be installed).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 DIRS contains 'raw/local' and 'raw/drop'
- [ ] #2 GITKEEP_DIRS contains 'raw/local' and 'raw/drop'
- [ ] #3 install_skills installs adopt_drop.py to .claude/skills/inbox-fetcher/scripts/adopt_drop.py
- [ ] #4 print_done banner references raw/drop/ not raw/papers/ in step 2
- [ ] #5 Running python3 init_vault.py --here on a fresh directory creates raw/local/.gitkeep and raw/drop/.gitkeep
- [ ] #6 Running python3 init_vault.py --here installs both fetch_inbox.py and adopt_drop.py into .claude/skills/inbox-fetcher/scripts/
<!-- AC:END -->
