---
id: TASK-0099.01
title: 'A3: Add review: block to vault.config.yml and _DEFAULTS'
status: To Do
assignee: []
created_date: '2026-05-30 23:33'
labels: []
dependencies: []
modified_files:
  - vault.config.yml
  - skills/shared/vault_state.py
parent_task_id: TASK-0099
priority: medium
ordinal: 1000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
commands/review.md reads vault.config.review.max_faithfulness_pages (defaults to 10 if absent), but there is no review: section in vault.config.yml or vault_state.py::_DEFAULTS. The knob is invisible to users customizing the config. All other config sections including LLM-layer-only ones (ingest:, lint auto-trigger keys) are in _DEFAULTS — review: must match that convention.

**Files to modify:**
- vault.config.yml — append review: block after drop_zone: section
- skills/shared/vault_state.py — add "review": {"max_faithfulness_pages": 10} after "drop_zone" in _DEFAULTS dict (lines 71–75)

**Exact addition to vault.config.yml** (append at end of file):
```yaml

# ── Keys below are honored by the LLM/command layer, NOT by any script ────────
review:
  # Max pages to run the claim-faithfulness check against per /review run.
  # Increase for thorough audits; decrease to control token spend.
  max_faithfulness_pages: 10
```

**Exact addition to vault_state.py _DEFAULTS** (after drop_zone block, before closing `}`):
```python
    "review": {
        "max_faithfulness_pages": 10,
    },
```

**Verify:** `python -m pytest tests/test_vault_state.py -v` passes. `vault_state.load_config` on a temp dir resolves `review.max_faithfulness_pages` to 10.

Full step-by-step: features/plans/2026-05-31-llm-wiki-correctness-docs-plan.md Task 1
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 vault.config.yml has a review: block with max_faithfulness_pages: 10, annotated LLM-layer-only
- [ ] #2 _DEFAULTS in vault_state.py has a review: entry matching
- [ ] #3 pytest tests/test_vault_state.py passes with no regressions
<!-- AC:END -->
