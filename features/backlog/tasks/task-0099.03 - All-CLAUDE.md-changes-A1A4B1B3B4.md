---
id: TASK-0099.03
title: All CLAUDE.md changes (A1+A4+B1+B3+B4)
status: To Do
assignee: []
created_date: '2026-05-30 23:34'
labels: []
dependencies:
  - TASK-0099.01
  - TASK-0099.02
modified_files:
  - CLAUDE.md
parent_task_id: TASK-0099
priority: high
ordinal: 3000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Eight targeted edits to CLAUDE.md in a single pass. All changes are independent sections of the same file — batch them to avoid partial-state commits. Full before/after strings for every edit are in the plan.

**Edits (in order to apply):**

1. **B3 — Template note** (after `## Vault structure` heading): add blockquote note that `.claude/` paths refer to the deployed vault, not this template repo.

2. **B1 — Tiered invariants** (replace lines 42–58): Replace `## Six invariants — never break these` with `## Invariants and operating rules` containing `### Hard invariants — never break these` (3 numbered: raw immutable, cite sources, paraphrase) and `### Operating rules` (4 bullets: user curates, ≤15 files, update index+log, shareable views frozen). This reunites the rule missing from CLAUDE.md (shareable frozen) and the rule missing from user docs (update index+log).

3. **A4 — Heading rename**: `## Twelve operations` → `## Operations`

4. **B1 consequence — FORGET cross-refs**: Replace `(invariant #6)` → `(operating rule: update after writes)` and `exceeds invariant #5` → `exceeds the 15-file operating rule`

5. **B1 consequence — MERGE cross-ref**: Replace `(Invariant #5)` → `(15-file operating rule)`

6. **B4 — Dispatch table paths**: Fix 4 short paths to full repo-relative: `scripts/fetch_inbox.py` → `skills/inbox-fetcher/scripts/fetch_inbox.py`, etc. Full table in plan Task 3 Step 6.

7. **A1 — Session-start compass conditional** (replace step 3 lines 357–359): Make compass.md read explicitly conditional — check if file exists first; if absent suggest /reflect; if present read updated field.

8. **B4 — Tooling separator**: Add `<!-- ───── Tooling config below — not part of the vault contract ───── -->` before the Backlog MCP block.

**Files to modify:** CLAUDE.md only.

**Verify:** Run the inline Python assertion script from plan Task 3 Step 9 to confirm all strings changed.

Full step-by-step with exact strings: features/plans/2026-05-31-llm-wiki-correctness-docs-plan.md Task 3
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 '## Six invariants' no longer present; '## Invariants and operating rules' present with Hard invariants and Operating rules subsections
- [ ] #2 '## Twelve operations' renamed to '## Operations'
- [ ] #3 Dispatch table paths are full repo-relative paths (skills/inbox-fetcher/scripts/...)
- [ ] #4 Session-start step 3 explicitly checks if compass.md exists before attempting to read it
- [ ] #5 Invariant #5 and #6 cross-references in FORGET and MERGE sections replaced with prose
- [ ] #6 Tooling separator comment added before Backlog MCP block
- [ ] #7 Template-vs-vault note present after ## Vault structure heading
<!-- AC:END -->
