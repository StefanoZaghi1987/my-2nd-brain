---
id: TASK-0060.05
title: CLAUDE.md — vault structure diagram and local-md source type branch
status: To Do
assignee: []
created_date: '2026-05-29 06:57'
labels: []
dependencies: []
documentation:
  - features/specs/2026-05-29-markdown-drop-ingest-design.md
modified_files:
  - CLAUDE.md
parent_task_id: TASK-0060
ordinal: 5000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Update `CLAUDE.md` with two changes:

**1. Vault structure diagram** — update the `raw/` section to reflect that `raw/local/` holds both PDFs and Markdown files:

```
raw/
  web/<slug>/         Web articles converted to markdown
  papers/<slug>/      PDFs fetched via URL
  local/<slug>/       PDFs or Markdown files copy-pasted by the user
  drop/               Drop zone — paste files here; emptied by /ingest
```

**2. INGEST — source type branches** — add a `local-md` branch after the existing `local-pdf` branch:

```
**Local Markdown files** (`raw/local/<slug>/index.md` with `fetch_method: local-md`):
1. Read `index.md` — get `title`, `source_url` (if present), `tags`, `note`.
2. Read `content.md` in full (plain text; no page limit).
3. Infer real title from content if better than index.md title.
4. Write `wiki/sources/<slug>.md` — omit `source_url` field if not present.
   Use `source_path: raw/local/<slug>/` and `fetch_method: local-md` in frontmatter.
5. Carry `tags` and `note` as with other source types.
```

**Context:** `CLAUDE.md` is the primary LLM instruction file for the vault. Read the full INGEST section before editing to match its tone and structure exactly. Do not change anything outside the vault structure diagram and the INGEST source type branches.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Vault structure diagram raw/local/ description updated to mention both PDFs and Markdown files
- [ ] #2 local-md source type branch added to INGEST section after local-pdf branch
- [ ] #3 local-md branch describes reading content.md in full with no page limit
- [ ] #4 local-md branch instructs omitting source_url when not present
- [ ] #5 No other sections of CLAUDE.md are modified
<!-- AC:END -->
