---
id: TASK-0060.06
title: lint.py — update check_drop_zone to detect .md files
status: Done
assignee: []
created_date: '2026-05-29 06:57'
updated_date: '2026-05-29 08:06'
labels: []
dependencies: []
documentation:
  - features/specs/2026-05-29-markdown-drop-ingest-design.md
modified_files:
  - skills/vault-linter/scripts/lint.py
  - tests/test_lint.py
parent_task_id: TASK-0060
ordinal: 6000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Update `check_drop_zone()` in `skills/vault-linter/scripts/lint.py` to report `.md` files in the drop zone alongside `.pdf` files.

**Current code (lines 372–392):** filters `p.suffix.lower() == ".pdf"` only. Extend to detect any file whose extension is in a supported set `{".pdf", ".md"}`.

**Updated finding detail format:**
- Both types present: `Drop zone has 2 PDF(s) and 1 Markdown file(s) - run /ingest to adopt them.`
- PDFs only: `Drop zone has 2 PDF(s) - run /ingest to adopt them.` (unchanged)
- MDs only: `Drop zone has 1 Markdown file(s) - run /ingest to adopt them.`

The finding check ID (`drop_zone_not_empty`), severity (`advisory`), and the trigger condition (non-empty drop zone) are unchanged.

**Constraint:** Do not hardcode the extension set in two places — define it once (e.g. as a module-level constant or local variable) so it stays in sync with `adopt_drop.py`'s `HANDLERS` conceptually. A comment referencing `adopt_drop.py HANDLERS` is sufficient; no import needed.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 check_drop_zone detects .md files in the drop zone
- [ ] #2 Finding detail message reports PDF and Markdown counts separately when both present
- [ ] #3 Finding detail message reports only the present type when only one type present
- [ ] #4 Existing advisory severity and drop_zone_not_empty check ID unchanged
- [ ] #5 check_drop_zone returns [] when drop zone is empty or disabled (unchanged)
- [ ] #6 Unit test or existing test updated to cover .md detection
<!-- AC:END -->
